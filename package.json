{
  "name": "netfy",
  "version": "1.0.1",
  "description": "Netfy Edge Function HTTP relay for your own upstream service",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  }
}
